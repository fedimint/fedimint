use fedimint_core::core::{DynModuleConsensusItem as ModuleConsensusItem, ModuleInstanceId};
use fedimint_core::encoding::{Decodable, Encodable};
use fedimint_core::module::{Amounts, CoreConsensusVersion, ModuleConsensusVersion};
use serde::{Deserialize, Serialize};
use serde_json::Value;

use crate::secp256k1::PublicKey;
use crate::transaction::Transaction;

/// All the items that may be produced during a consensus epoch
#[derive(Debug, Clone, Eq, PartialEq, Hash, Encodable, Decodable)]
pub enum ConsensusItem {
    /// Threshold sign the epoch history for verification via the API
    Transaction(Transaction),
    /// Any data that modules require consensus on
    Module(ModuleConsensusItem),
    /// A peer vote to activate a newer consensus version for a module.
    ModuleConsensusVersion(ModuleConsensusVersionVote),
    /// A peer vote to activate a newer core consensus version.
    CoreConsensusVersion(CoreConsensusVersion),
    /// A peer vote for core consensus unix time.
    CoreUnixTime(ConsensusUnixTime),
    /// A peer vote for a module fee consensus schedule.
    ModuleFeeConsensus(ModuleFeeConsensusVote),
    /// A peer vote to pay accrued fees out to a guardian.
    FeePayoutVoucher(FeePayoutVoucherVote),
    /// Allows us to add new items in the future without crashing old clients
    /// that try to interpret the session log.
    #[encodable_default]
    Default { variant: u64, bytes: Vec<u8> },
}

#[derive(Debug, Copy, Clone, Eq, PartialEq, Hash, Encodable, Decodable)]
pub struct ModuleConsensusVersionVote {
    pub module_instance_id: ModuleInstanceId,
    pub version: ModuleConsensusVersion,
}

#[derive(Debug, Copy, Clone, Eq, PartialEq, Serialize, Deserialize, Encodable, Decodable)]
pub struct ModuleConsensusVersionRequest {
    pub module_instance_id: ModuleInstanceId,
}

#[derive(Debug, Copy, Clone, Eq, PartialEq, Serialize, Deserialize, Encodable, Decodable)]
pub struct ActivateModuleConsensusVersionRequest {
    pub module_instance_id: ModuleInstanceId,
    pub version: Option<ModuleConsensusVersion>,
}

#[derive(Debug, Copy, Clone, Eq, PartialEq, Serialize, Deserialize, Encodable, Decodable)]
pub struct ActivateCoreConsensusVersionRequest {
    pub version: Option<CoreConsensusVersion>,
}

#[derive(
    Debug,
    Default,
    Copy,
    Clone,
    Eq,
    PartialEq,
    Ord,
    PartialOrd,
    Hash,
    Serialize,
    Deserialize,
    Encodable,
    Decodable,
)]
pub struct ConsensusUnixTime(pub u64);

#[derive(Debug, Clone, Eq, PartialEq, Hash, Encodable, Decodable)]
pub struct ModuleFeeConsensusVote {
    pub module_instance_id: ModuleInstanceId,
    pub fee_consensus: Vec<u8>,
}

#[derive(Debug, Copy, Clone, Eq, PartialEq, Serialize, Deserialize, Encodable, Decodable)]
pub struct ModuleFeeConsensusRequest {
    pub module_instance_id: ModuleInstanceId,
}

/// A peer's vote to pay `amounts` of the accrued fees out to `claimant`.
///
/// The vote a guardian submits to propose a payout is the same item the other
/// guardians submit to approve it: a payout is approved once a threshold of
/// peers has voted for byte-identical terms.
///
/// A vote for [`Amounts::ZERO`] retracts the peer's earlier vote, so a stale
/// proposal cannot be resurrected months later by a late approval.
#[derive(Debug, Clone, Eq, PartialEq, Hash, Encodable, Decodable)]
pub struct FeePayoutVoucherVote {
    /// Key permitted to spend the voucher, generated by the claiming
    /// guardian's client.
    ///
    /// Doubles as the voucher's identity. Clients generate a fresh key per
    /// voucher, which is what makes it unique.
    pub claimant: PublicKey,
    /// What the voucher is worth, in any combination of units.
    pub amounts: Amounts,
}

#[derive(Debug, Clone, Eq, PartialEq, Serialize, Deserialize, Encodable, Decodable)]
pub struct SetModuleFeeConsensusRequest {
    pub module_instance_id: ModuleInstanceId,
    pub fee_consensus: Vec<u8>,
}

#[derive(Debug, Clone, Eq, PartialEq, Serialize, Deserialize)]
pub struct SetModuleFeeConsensusJsonRequest {
    pub module_instance_id: ModuleInstanceId,
    pub fee_consensus: Value,
}

#[derive(Debug, Clone, Eq, PartialEq, Serialize, Deserialize, Encodable, Decodable)]
pub struct CurrentFeeConsensus {
    pub fee_consensus: Vec<u8>,
    pub active_since: ConsensusUnixTime,
}

#[derive(Debug, Clone, Eq, PartialEq, Serialize, Deserialize)]
pub struct CurrentFeeConsensusJson {
    pub fee_consensus: Value,
    pub active_since: ConsensusUnixTime,
}
