# Keep embedded-shell analysis enabled for new workflows while isolating
# pre-existing debt in the two large legacy workflows below.
paths:
  ".github/workflows/ci-nix.yml":
    ignore:
      - "shellcheck reported issue in this script: SC(2046|2086|2155):"
  ".github/workflows/codex-review.yml":
    ignore:
      - "shellcheck reported issue in this script: SC(2016|2129):"
