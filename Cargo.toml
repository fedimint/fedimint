[workspace]

members = [
    "config",
    "crypto/musig",
    "crypto/tbs",
    "database",
    "fedimint",
    "fediwallet",
    "ln-gateway",
    "minimint",
    "minimint-derive",
    "mint-api",
    "mint-client",
]

[patch.crates-io]
bitcoin_hashes = { git = 'https://github.com/elsirion/bitcoin_hashes', branch = 'minimint' }