[package]
name = "fedimint-wallet-client"
version = { workspace = true }
authors = ["The Fedimint Developers"]
edition = "2021"
description = "fedimint-wallet is a n on-chain bitcoin wallet module. It uses a key-value store and is not a standard HD wallet."
license = "MIT"
readme = "../../README.md"
repository = "https://github.com/fedimint/fedimint"

[package.metadata.docs.rs]
rustc-args = ["--cfg", "tokio_unstable"]

[lib]
name = "fedimint_wallet_client"
path = "src/lib.rs"

[features]
default =[]
cli = ["dep:clap" ]

[dependencies]
anyhow = { workspace = true }
aquamarine = "0.5.0"
async-stream = "0.3.5"
async-trait = { workspace = true }
bitcoin = { workspace = true }
clap = { workspace = true, optional = true }
erased-serde = { workspace = true }
fedimint-api-client = { workspace = true }
fedimint-client = { workspace = true }
fedimint-core = { workspace = true }
fedimint-logging = { workspace = true }
fedimint-wallet-common = { version = "=0.4.0-rc.2", path = "../fedimint-wallet-common" }
futures = { workspace = true }
rand = { workspace = true }
secp256k1 = { version = "0.27.0", features = [ "serde" ] }
serde = { workspace = true }
serde_json = { workspace = true }
strum = { workspace = true }
strum_macros = { workspace = true }
tracing = { workspace = true }

[target.'cfg(not(target_family = "wasm"))'.dependencies]
fedimint-bitcoind = { version = "=0.4.0-rc.2", path = "../../fedimint-bitcoind", default-features = false, features = ["esplora-client", "bitcoincore-rpc"] }
tokio = { version = "1.38.0", features = ["full", "tracing"] }

[target.'cfg(target_family = "wasm")'.dependencies]
fedimint-bitcoind = { version = "=0.4.0-rc.2", path = "../../fedimint-bitcoind", default-features = false, features = ["esplora-client"] }
tokio = { version = "1.38.0", features = [ "sync", "rt"] }

[dev-dependencies]
tracing-subscriber = { version = "0.3.18", features = [ "env-filter" ] }
