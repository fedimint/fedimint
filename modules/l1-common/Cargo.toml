[package]
name = "fedimint-l1-common"
version = "0.1.0"
authors = ["The Fedimint Developers"]
edition = "2021"
description = "Fedimint l1 (on-chain) module (common)."
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "fedimint_l1_common"
path = "src/lib.rs"

[dependencies]
fedimint-api = { path = "../../fedimint-api" }
tracing ="0.1.37"
